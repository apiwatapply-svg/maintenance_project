{
  "name": "maintenance-project-backend",
  "version": "0.1.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.js",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "latest",
    "dotenv": "latest",
    "express": "latest",
    "mssql": "latest",
    "multer": "^2.1.1"
  }
}
